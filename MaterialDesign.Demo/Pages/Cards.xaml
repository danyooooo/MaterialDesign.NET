<UserControl x:Class="MaterialDesign.Demo.Pages.Cards"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:MaterialDesign.NET.Helpers;assembly=MaterialDesign.NET"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="Cards" Style="{DynamicResource MdSysTypescale.DisplaySmall}" Margin="0,0,0,8" />
            <TextBlock Text="Cards contain content and actions about a single subject." Style="{DynamicResource MdSysTypescale.BodyLarge}" Margin="0,0,0,32" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}" TextWrapping="Wrap" />

            <!-- Elevated Card -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="Elevated Card" Style="{DynamicResource MdSysTypescale.HeadlineSmall}" Margin="0,0,0,16" />
                <Border Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="12" Padding="24">
                    <StackPanel>
                        <ContentControl Style="{DynamicResource MaterialDesignElevatedCardStyle}" Width="200" Height="120" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Elevated Card" Style="{DynamicResource MdSysTypescale.TitleMedium}" HorizontalAlignment="Center" />
                                <TextBlock Text="Level 1 Shadow" Style="{DynamicResource MdSysTypescale.BodyMedium}" HorizontalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}" />
                            </StackPanel>
                        </ContentControl>
                        
                        <Expander Header="Source Code">
                            <TextBox Style="{DynamicResource MaterialDesignOutlinedTextBoxStyle}"
                                     helpers:TextFieldAssist.Label="XAML"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Height="80"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Text="&lt;ContentControl Style=&quot;{DynamicResource MaterialDesignElevatedCardStyle}&quot;&gt;&#x0a;    &lt;!-- Content --&gt;&#x0a;&lt;/ContentControl&gt;" />
                        </Expander>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Filled Card -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="Filled Card" Style="{DynamicResource MdSysTypescale.HeadlineSmall}" Margin="0,0,0,16" />
                <Border Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="12" Padding="24">
                    <StackPanel>
                        <ContentControl Style="{DynamicResource MaterialDesignFilledCardStyle}" Width="200" Height="120" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Filled Card" Style="{DynamicResource MdSysTypescale.TitleMedium}" HorizontalAlignment="Center" />
                                <TextBlock Text="Surface Highest" Style="{DynamicResource MdSysTypescale.BodyMedium}" HorizontalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}" />
                            </StackPanel>
                        </ContentControl>

                        <Expander Header="Source Code">
                            <TextBox Style="{DynamicResource MaterialDesignOutlinedTextBoxStyle}"
                                     helpers:TextFieldAssist.Label="XAML"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Height="80"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Text="&lt;ContentControl Style=&quot;{DynamicResource MaterialDesignFilledCardStyle}&quot;&gt;&#x0a;    &lt;!-- Content --&gt;&#x0a;&lt;/ContentControl&gt;" />
                        </Expander>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Outlined Card -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="Outlined Card" Style="{DynamicResource MdSysTypescale.HeadlineSmall}" Margin="0,0,0,16" />
                <Border Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="12" Padding="24">
                    <StackPanel>
                        <ContentControl Style="{DynamicResource MaterialDesignOutlinedCardStyle}" Width="200" Height="120" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Outlined Card" Style="{DynamicResource MdSysTypescale.TitleMedium}" HorizontalAlignment="Center" />
                                <TextBlock Text="Border Variant" Style="{DynamicResource MdSysTypescale.BodyMedium}" HorizontalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}" />
                            </StackPanel>
                        </ContentControl>

                        <Expander Header="Source Code">
                             <TextBox Style="{DynamicResource MaterialDesignOutlinedTextBoxStyle}"
                                     helpers:TextFieldAssist.Label="XAML"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Height="80"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Text="&lt;ContentControl Style=&quot;{DynamicResource MaterialDesignOutlinedCardStyle}&quot;&gt;&#x0a;    &lt;!-- Content --&gt;&#x0a;&lt;/ContentControl&gt;" />
                        </Expander>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Elevation / Shadows -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="Elevation" Style="{DynamicResource MdSysTypescale.HeadlineSmall}" Margin="0,0,0,16" />
                <TextBlock Text="Elevation involves utilizing shadows to create visual depth." Style="{DynamicResource MdSysTypescale.BodyMedium}" Margin="0,0,0,16" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}" />
                <Border Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="12" Padding="24">
                    <StackPanel>
                        <WrapPanel Margin="0,0,0,16">
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level0">
                                <TextBlock Text="Level 0" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level1">
                                <TextBlock Text="Level 1" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level2">
                                <TextBlock Text="Level 2" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level3">
                                <TextBlock Text="Level 3" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level4">
                                <TextBlock Text="Level 4" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <Border Width="100" Height="100" Background="{DynamicResource MdSysColor.SurfaceContainerLow}" Margin="0,0,24,24" CornerRadius="12" helpers:ShadowAssist.Elevation="Level5">
                                <TextBlock Text="Level 5" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </WrapPanel>

                        <Expander Header="Source Code">
                            <TextBox Style="{DynamicResource MaterialDesignOutlinedTextBoxStyle}"
                                     helpers:TextFieldAssist.Label="XAML"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Height="80"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Text="&lt;Border helpers:ShadowAssist.Elevation=&quot;Level3&quot; ... /&gt;" />
                        </Expander>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
