<UserControl x:Class="MaterialDesign.Demo.Pages.Snackbars"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaterialDesign.Demo.Pages"
             xmlns:controls="clr-namespace:MaterialDesign.NET.Controls;assembly=MaterialDesign.NET"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" MaxWidth="800" HorizontalAlignment="Stretch">
                <TextBlock Text="Snackbars" Style="{DynamicResource MdSysTypescale.DisplayMedium}" Margin="0,0,0,24" />
                <TextBlock Text="Snackbars provide brief messages about app processes at the bottom of the screen." 
                           Style="{DynamicResource MdSysTypescale.BodyMedium}" Margin="0,0,0,24" />

                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Button Content="Show Simple Snackbar" Click="ShowSimpleSnackbar_Click" Margin="0,0,16,0" />
                    <Button Content="Show Snackbar with Action" Click="ShowActionSnackbar_Click" />
                </StackPanel>
                
                 <TextBlock Text="Click the buttons above to trigger the snackbar below." 
                           Style="{DynamicResource MdSysTypescale.BodySmall}" Opacity="0.6" />

            </StackPanel>
        </ScrollViewer>

        <!-- Snackbar Host Area -->
        <controls:Snackbar x:Name="MainSnackbar" Grid.Row="1" Message="Welcome to Material Design 3!" />
    </Grid>
</UserControl>
