<UserControl x:Class="MaterialDesign.Demo.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MaterialDesign.NET.Controls;assembly=MaterialDesign.NET"
             xmlns:helpers="clr-namespace:MaterialDesign.NET.Helpers;assembly=MaterialDesign.NET"
             mc:Ignorable="d"
             d:DesignHeight="1200" d:DesignWidth="1000">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Hero Section -->
                <Border Background="{DynamicResource MdSysColor.PrimaryContainer}" Padding="48,64,48,64">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Material Design 3" Style="{DynamicResource MdSysTypescale.DisplayLarge}" Foreground="{DynamicResource MdSysColor.OnPrimaryContainer}" Margin="0,0,0,16" />
                            <TextBlock Text="A complete .NET WPF implementation of Google's Material Design 3 specification" Style="{DynamicResource MdSysTypescale.HeadlineSmall}" Foreground="{DynamicResource MdSysColor.OnPrimaryContainer}" Margin="0,0,0,32" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{DynamicResource MaterialDesignFilledButtonStyle}" Content="Explore Components" HorizontalAlignment="Left" Width="180" Margin="0,0,12,0"/>
                                <Button Style="{DynamicResource MaterialDesignTonalButtonStyle}" Content="View on GitHub" HorizontalAlignment="Left" Width="160"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <controls:SymbolIcon Symbol="palette" Variant="Rounded" Filled="True" Width="120" Height="120" Foreground="{DynamicResource MdSysColor.Primary}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Quick Actions -->
                <Border Background="{DynamicResource MdSysColor.SurfaceContainerHighest}" Padding="32,24,32,24" Margin="0,24,0,0">
                    <StackPanel>
                        <TextBlock Text="Quick Actions" Style="{DynamicResource MdSysTypescale.TitleLarge}" Margin="0,0,0,16" />
                        <WrapPanel>
                            <Button Style="{DynamicResource MaterialDesignFilledButtonStyle}" Content="Toggle Theme" Margin="0,0,8,8" Width="140" Click="ToggleTheme_Click"/>
                            <Button Style="{DynamicResource MaterialDesignOutlinedButtonStyle}" Content="Show Snackbar" Margin="0,0,8,8" Width="140" Click="ShowSnackbar_Click"/>
                            <Button Style="{DynamicResource MaterialDesignTonalButtonStyle}" Content="Show Dialog" Margin="0,0,8,8" Width="140" Click="ShowDialog_Click"/>
                            <Button Style="{DynamicResource MaterialDesignTextButtonStyle}" Content="Show Notification" Margin="0,0,8,8" Width="140" Click="ShowNotification_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Components Overview Grid -->
                <StackPanel Margin="48,32,48,32">
                    <TextBlock Text="Component Showcase" Style="{DynamicResource MdSysTypescale.HeadlineMedium}" Margin="0,0,0,32" />

                    <!-- Action Components -->
                    <TextBlock Text="Action Components" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <WrapPanel Margin="0,0,0,32">
                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Buttons" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <WrapPanel>
                                    <Button Content="Filled" Style="{DynamicResource MaterialDesignFilledButtonStyle}" Margin="0,0,4,4"/>
                                    <Button Content="Outlined" Style="{DynamicResource MaterialDesignOutlinedButtonStyle}" Margin="0,0,4,4"/>
                                    <Button Content="Text" Style="{DynamicResource MaterialDesignTextButtonStyle}" Margin="0,0,4,4"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Icon Buttons" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <WrapPanel>
                                    <Button Style="{DynamicResource MaterialDesignIconButtonStyle}" Margin="0,0,4,4">
                                        <controls:PackIcon Kind="Settings" Width="24" Height="24"/>
                                    </Button>
                                    <Button Style="{DynamicResource MaterialDesignFilledIconButtonStyle}" Margin="0,0,4,4">
                                        <controls:PackIcon Kind="Add" Width="24" Height="24"/>
                                    </Button>
                                    <Button Style="{DynamicResource MaterialDesignOutlinedIconButtonStyle}" Margin="0,0,4,4">
                                        <controls:PackIcon Kind="Edit" Width="24" Height="24"/>
                                    </Button>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Floating Action Button" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <WrapPanel>
                                    <Button Style="{DynamicResource MaterialDesignFABStyle}" Width="56" Height="56" Margin="0,0,8,0">
                                        <controls:PackIcon Kind="Add" Width="24" Height="24"/>
                                    </Button>
                                    <Button Style="{DynamicResource MaterialDesignExtendedFABStyle}" Tag="Create New" HorizontalAlignment="Left" MinWidth="140">
                                        <controls:PackIcon Kind="Add" Width="24" Height="24"/>
                                    </Button>
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Selection Components -->
                    <TextBlock Text="Selection Components" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <WrapPanel Margin="0,0,0,32">
                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Selection Controls" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel>
                                    <CheckBox Content="Checkbox" Style="{DynamicResource MaterialDesignCheckboxStyle}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Checked" Style="{DynamicResource MaterialDesignCheckboxStyle}" IsChecked="True" Margin="0,0,0,8"/>
                                    <ToggleButton Content="Switch" Style="{DynamicResource MaterialDesignSwitchStyle}" Margin="0,0,0,8"/>
                                    <RadioButton Content="Radio" Style="{DynamicResource MaterialDesignRadioButtonStyle}" GroupName="demo" Margin="0,0,0,8"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Chips" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <WrapPanel>
                                    <Button Content="Assist Chip" Style="{DynamicResource MaterialDesignAssistChipStyle}" Margin="0,0,4,4" helpers:ChipAssist.LeadingIcon="Link"/>
                                    <ToggleButton Content="Filter Chip" Style="{DynamicResource MaterialDesignFilterChipStyle}" Margin="0,0,4,4" helpers:ChipAssist.LeadingIcon="Check"/>
                                    <Button Content="Input Chip" Style="{DynamicResource MaterialDesignInputChipStyle}" Margin="0,0,4,4" helpers:ChipAssist.TrailingIcon="Close"/>
                                    <Button Content="Suggestion" Style="{DynamicResource MaterialDesignSuggestionChipStyle}" Margin="0,0,4,4"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Tabs" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TabControl Style="{DynamicResource MaterialDesignTabControlStyle}" Margin="0,0,0,8">
                                    <TabItem Header="Tab 1" Style="{DynamicResource MaterialDesignTabItemStyle}">
                                        <TextBlock Text="Content 1" Margin="16"/>
                                    </TabItem>
                                    <TabItem Header="Tab 2" Style="{DynamicResource MaterialDesignTabItemStyle}">
                                        <TextBlock Text="Content 2" Margin="16"/>
                                    </TabItem>
                                    <TabItem Header="Tab 3" Style="{DynamicResource MaterialDesignTabItemStyle}">
                                        <TextBlock Text="Content 3" Margin="16"/>
                                    </TabItem>
                                </TabControl>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Input Components -->
                    <TextBlock Text="Input Components" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <WrapPanel Margin="0,0,0,32">
                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Text Fields" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBox Style="{DynamicResource MaterialDesignFilledTextBoxStyle}" helpers:TextFieldAssist.Label="Filled" Margin="0,0,0,12"/>
                                <TextBox Style="{DynamicResource MaterialDesignOutlinedTextBoxStyle}" helpers:TextFieldAssist.Label="Outlined" Margin="0,0,0,8"/>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Menus &amp; Select" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <ComboBox Style="{DynamicResource MaterialDesignFilledComboBoxStyle}" helpers:TextFieldAssist.Label="Filled" Margin="0,0,0,12">
                                    <ComboBoxItem Content="Option 1"/>
                                    <ComboBoxItem Content="Option 2"/>
                                    <ComboBoxItem Content="Option 3"/>
                                </ComboBox>
                                <ComboBox Style="{DynamicResource MaterialDesignOutlinedComboBoxStyle}" helpers:TextFieldAssist.Label="Outlined">
                                    <ComboBoxItem Content="Option 1"/>
                                    <ComboBoxItem Content="Option 2"/>
                                    <ComboBoxItem Content="Option 3"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Sliders" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <Slider Style="{DynamicResource MaterialDesignSliderStyle}" Minimum="0" Maximum="100" Value="50" Margin="0,0,0,16"/>
                                <Slider Style="{DynamicResource MaterialDesignSliderStyle}" Minimum="0" Maximum="100" Value="30" TickFrequency="10" IsSnapToTickEnabled="True" Margin="0,0,0,8"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Display Components -->
                    <TextBlock Text="Display &amp; Feedback" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <WrapPanel Margin="0,0,0,32">
                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Cards" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <ContentControl Style="{DynamicResource MaterialDesignElevatedCardStyle}" Padding="16" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Elevated Card" Style="{DynamicResource MdSysTypescale.TitleSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="Cards contain content and actions" Style="{DynamicResource MdSysTypescale.BodySmall}"/>
                                    </StackPanel>
                                </ContentControl>
                                <ContentControl Style="{DynamicResource MaterialDesignFilledCardStyle}" Padding="16" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Filled Card" Style="{DynamicResource MdSysTypescale.TitleSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="Different visual emphasis" Style="{DynamicResource MdSysTypescale.BodySmall}"/>
                                    </StackPanel>
                                </ContentControl>
                                <ContentControl Style="{DynamicResource MaterialDesignOutlinedCardStyle}" Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="Outlined Card" Style="{DynamicResource MdSysTypescale.TitleSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="Lower visual emphasis" Style="{DynamicResource MdSysTypescale.BodySmall}"/>
                                    </StackPanel>
                                </ContentControl>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Progress" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <ProgressBar Style="{DynamicResource MaterialDesignLinearProgressBar}" IsIndeterminate="True" Margin="0,0,0,16"/>
                                <ProgressBar Style="{DynamicResource MaterialDesignLinearProgressBar}" Value="60" Margin="0,0,0,16"/>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Badges &amp; Notifications" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <controls:Badge Content="🔔" BadgeContent="3" Style="{DynamicResource MaterialDesignBadgeStyle}" Margin="0,0,16,0"/>
                                    <controls:Badge Content="📧" BadgeContent="99+" Style="{DynamicResource MaterialDesignLargeBadgeStyle}"/>
                                </StackPanel>
                                <Border Background="{DynamicResource MdSysColor.InverseSurface}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                    <TextBlock Text="Snackbar message with action" Style="{DynamicResource MdSysTypescale.BodyMedium}" Foreground="{DynamicResource MdSysColor.InverseOnSurface}"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Navigation Components -->
                    <TextBlock Text="Navigation Components" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <WrapPanel Margin="0,0,0,32">
                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Navigation" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8"></StackPanel>
                                <Border Background="{DynamicResource MdSysColor.SecondaryContainer}" CornerRadius="16" Padding="12" Margin="0,0,0,8">
                                    <StackPanel Orientation="Horizontal"><TextBlock Text="Home" Style="{DynamicResource MdSysTypescale.LabelLarge}" Foreground="{DynamicResource MdSysColor.OnSecondaryContainer}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Width="280" Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="20" Margin="0,0,16,16">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12"><TextBlock Text="Lists" Style="{DynamicResource MdSysTypescale.TitleSmall}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <ListBox Style="{DynamicResource MaterialDesignListBoxStyle}" BorderThickness="0" ScrollViewer.CanContentScroll="False">
                                    <ListBoxItem Style="{DynamicResource MaterialDesignListBoxItemStyle}">
                                        <StackPanel Orientation="Horizontal"><TextBlock Text="Single-line list item"/>
                                        </StackPanel>
                                    </ListBoxItem>
                                    <ListBoxItem Style="{DynamicResource MaterialDesignTwoLineListBoxItemStyle}">
                                        <StackPanel>
                                            <TextBlock Text="Two-line list item"/>
                                            <TextBlock Text="Supporting text" Style="{DynamicResource MdSysTypescale.BodySmall}"/>
                                        </StackPanel>
                                    </ListBoxItem>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Typography Preview -->
                    <TextBlock Text="Typography Scale" Style="{DynamicResource MdSysTypescale.TitleMedium}" Foreground="{DynamicResource MdSysColor.Primary}" Margin="0,0,0,16" />
                    <Border Background="{DynamicResource MdSysColor.SurfaceContainer}" CornerRadius="{DynamicResource MdSysShape.CornerMedium}" Padding="24" Margin="0,0,0,32">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Display Large" Style="{DynamicResource MdSysTypescale.BodyLarge}" VerticalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                            <TextBlock Grid.Column="1" Text="The quick brown fox" Style="{DynamicResource MdSysTypescale.DisplayLarge}"/>

                            <TextBlock Grid.Row="1" Text="Headline Large" Style="{DynamicResource MdSysTypescale.BodyLarge}" VerticalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="The quick brown fox" Style="{DynamicResource MdSysTypescale.HeadlineLarge}"/>

                            <TextBlock Grid.Row="2" Text="Title Large" Style="{DynamicResource MdSysTypescale.BodyLarge}" VerticalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="The quick brown fox" Style="{DynamicResource MdSysTypescale.TitleLarge}"/>

                            <TextBlock Grid.Row="3" Text="Body Large" Style="{DynamicResource MdSysTypescale.BodyLarge}" VerticalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="The quick brown fox jumps over the lazy dog" Style="{DynamicResource MdSysTypescale.BodyLarge}"/>

                            <TextBlock Grid.Row="4" Text="Label Large" Style="{DynamicResource MdSysTypescale.BodyLarge}" VerticalAlignment="Center" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="THE QUICK BROWN FOX" Style="{DynamicResource MdSysTypescale.LabelLarge}"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Footer -->
                <Border Background="{DynamicResource MdSysColor.SurfaceContainerHighest}" Padding="32,24,32,24">
                    <StackPanel>
                        <TextBlock Text="Material Design 3 for .NET WPF" Style="{DynamicResource MdSysTypescale.TitleMedium}" Margin="0,0,0,8"/>
                        <TextBlock Text="Built with MaterialDesign.NET - A complete MD3 implementation" Style="{DynamicResource MdSysTypescale.BodyMedium}" Foreground="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
