<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MaterialDesign.NET.Controls">

    <!-- Navigation Rail Style (Targeting ListBox) -->
    <Style x:Key="MaterialDesignNavigationRailStyle" TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="{DynamicResource MdSysColor.Surface}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,12,12,12" /> <!-- Rail padding -->
        <Setter Property="Width" Value="80" /> <!-- Standard Rail width -->
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Navigation Rail Item Style (Targeting ListBoxItem) -->
    <Style x:Key="MaterialDesignNavigationRailItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,4"/> <!-- Spacing between items -->
        <Setter Property="Height" Value="56" /> <!-- Active Indicator Height -->
        <Setter Property="Width" Value="56" /> <!-- Active Indicator Width -->
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Foreground" Value="{DynamicResource MdSysColor.OnSurfaceVariant}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Grid>
                        <!-- Active Indicator (Pill Shape) -->
                        <Border x:Name="Indicator" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="{DynamicResource MdSysShape.CornerFull}"
                                SnapsToDevicePixels="true">
                            <local:Ripple CornerRadius="{DynamicResource MdSysShape.CornerFull}"
                                          Feedback="{TemplateBinding Foreground}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </local:Ripple>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                             <!-- We don't change background on hover for the item itself in MD3, usually just the state layer (Ripple handles this). 
                                  But we can add a subtle background if desired. 
                                  MD3 spec: Active indicator handles selection. Hover on inactive puts a state layer on the icon. 
                                  Let's keep it simple: Ripple handles hover visual. -->
                             <Setter Property="Foreground" Value="{DynamicResource MdSysColor.OnSurface}"/>
                        </Trigger>

                        <!-- Selected State -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MdSysColor.SecondaryContainer}"/>
                            <Setter Property="Foreground" Value="{DynamicResource MdSysColor.OnSecondaryContainer}"/>
                        </Trigger>
                        
                        <!-- Disabled State -->
                         <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.38"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
